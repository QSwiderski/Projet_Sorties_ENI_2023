{% extends 'base.html.twig' %}

{% block title %}{{ event.name }} - {{ parent() }}{% endblock %}

{% block body %}
    <div class="container">
        <h2>Evenement {{ event.name }}</h2>
        <p>nom : {{ event.name }}</p>
        <p>debut : {{ event.dateStart.format('Y-m-d H:i:s') }}</p>
        <p>clôture : {{ event.dateLimit.format('Y-m-d H:i:s') }}</p>
        <p>places : {{ event.peopleMax }}</p>
        <p>etat : //TODO etat </p>
        <p>organisateur : {{ event.organizer.name }}</p>
    </div>
{% endblock %}
