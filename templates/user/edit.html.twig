{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <h1>Modifier les infos utilisateur de {{ mail }}</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button>Valider</button>
    {{ form_end(form) }}

    <a href="{{ path('user_index') }}">Retour à la liste d'utilisateurs</a>

{% endblock %}
